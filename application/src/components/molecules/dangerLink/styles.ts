import { styled } from "styled-components";
import { LinkContainer } from "../../atoms/Link/styles";

export const DangerLink = styled(LinkContainer)`
  color: rgb(var(--link));
  font-size: var(--system-14-font-size);
`;
