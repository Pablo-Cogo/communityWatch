export const mongooseErrorMessages = {
  required: 'O campo "{PATH}" é obrigatório.',
  unique: 'Este valor já está em uso para o campo "{PATH}".',
  enum: 'O valor para o campo "{PATH}" não é válido.',
  custom: 'Erro de validação: {REASON}',
  // Adicione mais mensagens personalizadas aqui conforme necessário.
};
